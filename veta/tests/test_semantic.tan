# test_semantic.tan — VETA Semantic (semantic.tan) smoke test
# Kompilasyon: qemu-x86_64 TancElf <bu_dosya> <cikti>
# Çalıştırma:  qemu-x86_64 <cikti>
# Kural: mevcut TancElf binary'sini kullan; Faz 6 zemini (Storage + Query + Transaction + Event + Graph + Semantic hazır); QEMU throttle toleranı.

içe al "../libraries/concurrency/source/semantic.tan"

# Basit semantic testi
yaz("Semantic Test: Kavram tanımı aktif\n")
yaus("Ilgi durumu: ")
yaus(IlgiGuncelle(0, 1))
yaz("\n")