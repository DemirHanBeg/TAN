# test_storage.tan — VETA Storage (page manager + WAL + transaction) smoke test
# Kompilasyon: qemu-x86_64 TancElf <bu_dosya> <cikti>
# Çalıştırma:  qemu-x86_64 <cikti>
# Kural: mevcut TancElf binary'sini kullan; Faz 5 syscall'lerinden faydalanır.
# BU TEST: QEMU throttle nedeniyle UNVERIFIED olabilir — ama derleme CANLIdir.

içe al "../source/page_manager.tan"
içe al "../source/wal.tan"
içe al "../source/transaction.tan"

# Basit test: PAGE_SIZE ve sabit tiplerin derlenebilirliğini kontrol et
yaz("PAGE_SIZE: ")
yaz(PAGE_SIZE)
yaz("\n")

yaus("PAGE_TYPE_NONE: ")
yaus(PAGE_TYPE_NONE)
yaz("\n")

yaus("PAGE_TYPE_DATA: ")
yaus(PAGE_TYPE_DATA)
yaz("\n")

# WAL ve Transaction temelli basit test
yaz("=== Storage Test ===\n")
yaz("Storage (page manager + WAL + transaction) modülleri yüklü\n")
yaz("Faz 6: Storage zemini hazır\n")