# test_event.tan — VETA Event Sistemi smoke test
# Kompilasyon: qemu-x86_64 TancElf <bu_dosya> <cikti>
# Çalıştırma:  qemu-x86_64 <cikti>
# Kural: mevcut TancElf binary'sini kullan; Faz 6 zemini (Storage + Query + Transaction + Event hazır); QEMU throttle toleranı.

içe al "../libraries/concurrency/source/event.tan"

# Basit event testi
yaz("Event Sistemi Testi: Olay yayınlama mekanizması aktive edildi\n")
yaus("Event tipi: BASİC\n")
yaus("Aktif abone sayısı: ")
yaus(aktif_abone_sayisi(BASİC))
yaz("\n")