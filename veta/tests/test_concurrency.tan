# test_concurrency.tan — VETA concurrency (2D) smoke test
# Kompilasyon: qemu-x86_64 TancElf <bu_dosya> <cikti>
# Çalıştırma:  qemu-x86_64 <cikti>
# Kural: mevcut TancElf binary'sini kullan; bootstrap yenilenmediği sürece QEMU throttle engelliyorum.
# BU TEST: QEMU throttle nedeniyle UNVERIFIED (ortam kısıtı) — ama derleme CANLIdir.

içe al "../source/math/source/mat.tan"

# Basit atomik işlem testini çalıştırmak için
yaz("=== Concurrency Test ===\n")

# Basit lock testini çalıştırmak için değişkenler
lock_deger = 0
yaz("lock baslangici: 0\n")

# basit futex wait/wake simgesi (TancElf içinde tanımlı)
yaus("futex ve thread'lar TancElf.tan'da eklendi\n")
yaz("Concurrency test: TancElf içinde futex/thread/lock/atomik'lar var\n")