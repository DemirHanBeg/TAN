# test_wal.tan — VETA WAL smoke test.
# 2026-08-22: eski hali `../libraries/storage/source/wal.tan` (tasarım notu,
# sıfır kod) import ediyordu VE "yaus(" yazım hatası vardı — hiç derlenmiyordu.
# Gerçek, çalışan implementasyon artık kutuphane/Wal.tan. Asıl kapsamlı,
# otomatik regresyona dahil test: testler/storage_testleri.tan (13/13
# GEÇİYOR, TestAraclar.sh çalıştırır — commit/rollback dahil).

içe al "kutuphane/Wal.tan"

yaz("WAL baslatiliyor...")
wal = walAc("/tmp/veta_smoke_wal.wal")
yaz(metinBirlestir("baslangic lsn: ", metin(walSonLsn(wal))))
d = walKapat(wal)
yaz("WAL smoke test tamamlandi")
